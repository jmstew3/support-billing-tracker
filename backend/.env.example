# Database Configuration (Docker)
DB_HOST=mysql
DB_PORT=3306
DB_USER=***REMOVED***
DB_PASSWORD=***REMOVED***
DB_NAME=support_billing_tracker

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Note: When running with Docker Compose, the DB_HOST should be 'mysql' (the service name)
# When running locally without Docker, use 'localhost' and port 3307