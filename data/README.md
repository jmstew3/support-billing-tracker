# Data Directory Organization

This directory contains all data files for the Thad-Norman chat analysis project, organized by processing stage.

## 📁 Directory Structure

```
data/
├── 01_raw/                          # Golden Master Data - NEVER MODIFY
├── 02_processed/                    # Intermediate Processing Results
├── 03_final/                       # Application-Ready Data
└── README.md                       # This file
```

## 🔴 01_raw/ - Golden Master Data

**⚠️ CRITICAL: These files should NEVER be modified directly**

- `thad_norman_messages_complete.csv` - **PRIMARY SOURCE**
  - Contains raw iMessage export with actual message text
  - Includes iMessage artifacts (e.g., `+TGood`, `iINSDictionary`)
  - Use this file to regenerate all other data if needed
  - Size: ~502KB, 13,000+ messages

### archive/
- `thad_norman_all_messages.csv` - Incomplete export (rich text placeholders)
- `thad_norman_messages_full_export.csv` - Incomplete export (media placeholders)

## 🟡 02_processed/ - Intermediate Processing

**Generated files - safe to delete and regenerate**

- `thad_norman_messages_cleaned.csv` - Cleaned version of raw data
  - iMessage artifacts removed by `data_preprocessor.py`
  - Used as input for request extraction
  - Generated by: `python3 process.py --clean`

### archive/
- `thad_requests_categorized.csv` - Old manual categorization (obsolete)

## 🟢 03_final/ - Application Data

**Active application data - regenerated from processed files**

- `thad_requests_table.csv` - Final extracted requests
  - Used by React dashboard in frontend
  - Generated by: `python3 process.py --extract`
  - Format: date, time, month, request_type, category, description, urgency, effort

## 🚀 Data Pipeline Flow

```
01_raw/thad_norman_messages_complete.csv
    ↓ (data_preprocessor.py)
02_processed/thad_norman_messages_cleaned.csv  
    ↓ (request_extractor.py)
03_final/thad_requests_table.csv
    ↓ (copied to frontend/public/)
React Dashboard
```

## 🔧 Common Commands

### Regenerate All Data
```bash
# Clean raw data
python3 process.py --input data/01_raw/thad_norman_messages_complete.csv --clean

# Extract requests  
python3 process.py --input data/02_processed/thad_norman_messages_cleaned.csv --extract
```

### Reset to Golden Master
```bash
# If processing goes wrong, always start from:
data/01_raw/thad_norman_messages_complete.csv
```

## 📊 File Sizes & Status

| File | Size | Status | Purpose |
|------|------|--------|---------|
| `01_raw/thad_norman_messages_complete.csv` | 502KB | Golden Master | Never modify |
| `02_processed/thad_norman_messages_cleaned.csv` | 431KB | Generated | Safe to regenerate |
| `03_final/thad_requests_table.csv` | ~25KB | Generated | Used by app |

## ⚠️ Important Notes

1. **Always backup** `01_raw/` before major changes
2. **Never edit golden master files** directly
3. **Regenerate from source** if data looks corrupted
4. **Test changes** with small datasets first
5. **Document any new processing steps** in this README