# Thad Chat Environment Configuration Example
# Copy this file to .env.docker and update values for your environment

# ==============================================================================
# HOST CONFIGURATION
# ==============================================================================
# Set this to your server's IP address or hostname
# Examples: localhost, 192.168.1.100, 172.16.0.89, nas.local
APP_HOST=localhost

# ==============================================================================
# PORT CONFIGURATION
# ==============================================================================
# External ports for services (change if you have conflicts)
MYSQL_PORT=3307
BACKEND_PORT=3001
FRONTEND_PORT=5173
N8N_PORT=5678

# ==============================================================================
# PATH CONFIGURATION
# ==============================================================================
# Data directory path (relative or absolute)
# This is where your raw data, processed data, and final data are stored
DATA_PATH=./data

# MySQL data directory (for bind mount instead of named volume)
# This makes backups easier on NAS systems
MYSQL_DATA_PATH=./mysql_data

# ==============================================================================
# MYSQL DATABASE CONFIGURATION
# ==============================================================================
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_USER=thaduser
MYSQL_PASSWORD=thadpassword
MYSQL_DATABASE=thad_chat

# Internal MySQL settings (usually don't need to change)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=thad_chat

# ==============================================================================
# APPLICATION ENVIRONMENT
# ==============================================================================
# Options: development, production
NODE_ENV=development

# ==============================================================================
# API URLS (automatically constructed from APP_HOST and ports)
# ==============================================================================
# These are automatically built from the APP_HOST variable
# Only override if you have a custom setup (e.g., reverse proxy, HTTPS)
VITE_API_URL=http://${APP_HOST}:${BACKEND_PORT}/api
FRONTEND_URL=http://${APP_HOST}:${FRONTEND_PORT}

# ==============================================================================
# TWENTY CRM CONFIGURATION
# ==============================================================================
# Set these if you're using Twenty CRM integration
VITE_TWENTY_API_URL=https://twenny.peakonedigital.com/rest/supportTickets
VITE_TWENTY_API_TOKEN=your_twenty_api_token_here
VITE_TWENTY_USE_MOCK=true

# ==============================================================================
# N8N CONFIGURATION (optional)
# ==============================================================================
# Uncomment and set if you're using n8n automation
# N8N_USER=admin
# N8N_PASSWORD=your_n8n_password

# ==============================================================================
# PRODUCTION OVERRIDES
# ==============================================================================
# Uncomment these for production deployment
# NODE_ENV=production
# MYSQL_ROOT_PASSWORD=strong_production_root_password
# MYSQL_PASSWORD=strong_production_password
#
# For HTTPS (if using reverse proxy):
# VITE_API_URL=https://yourdomain.com/api
# FRONTEND_URL=https://yourdomain.com

# ==============================================================================
# MIGRATION NOTES
# ==============================================================================
# When moving to a new server (e.g., NAS):
# 1. Change APP_HOST to your new server's IP (e.g., 172.16.0.89)
# 2. Optionally adjust ports if there are conflicts
# 3. Set MYSQL_DATA_PATH to where you want MySQL data stored
# 4. Import your MySQL backup after starting the services
#
# Example for NAS deployment:
# APP_HOST=172.16.0.89
# MYSQL_DATA_PATH=/volume1/docker/thad-chat/mysql_data
# DATA_PATH=/volume1/docker/thad-chat/data